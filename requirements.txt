# Majster-AI / Car_Diagnostic_AI - full runtime requirement set.
#
# Prefer `pip install -e ".[all,dev]"` (see pyproject.toml) which lets you pick
# exactly the extras you need. This file installs everything, for the common
# case of a dedicated venv on a workshop laptop, a Raspberry Pi, or Termux.
#
# Termux / ARM note: chromadb and sentence-transformers are the two heavy
# entries here. If they will not build, drop them -- the RAG server degrades
# to an in-memory index with a dependency-free hash embedder and still works.

# --- core ---------------------------------------------------------------
pydantic>=2.6,<3
pydantic-settings>=2.2,<3
python-dotenv>=1.0

# --- vehicle interface (UDS over CAN) -----------------------------------
python-can>=4.3
udsoncan>=1.21
can-isotp>=2.0
pyserial>=3.5

# --- Model Context Protocol ---------------------------------------------
mcp>=1.2

# --- agent orchestration -------------------------------------------------
langgraph>=0.2.60
langchain-core>=0.3.20
langchain-anthropic>=0.3
langchain-ollama>=0.2
langchain-mcp-adapters>=0.1

# --- workshop-manual RAG -------------------------------------------------
chromadb>=0.5
pypdf>=4.0
langchain-text-splitters>=0.3
sentence-transformers>=3.0

# --- web search ----------------------------------------------------------
tavily-python>=0.5
ddgs>=6.0

# --- web UI backend ------------------------------------------------------
fastapi>=0.110
uvicorn[standard]>=0.27
websockets>=12

# --- development ---------------------------------------------------------
pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.24
black>=24.3
flake8>=7.0
httpx>=0.27
